{
  "name": "naver-clip-integration",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Reusable Naver Clip profile data and browser upload integration",
  "main": "src/index.js",
  "exports": "./src/index.js",
  "scripts": {
    "example": "node examples/upload.mjs",
    "check": "node --check src/index.js && node --check src/account.js && node --check src/naver-clip-client.js && node --check examples/upload.mjs",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "playwright": "^1.55.0"
  },
  "engines": {
    "node": ">=20"
  }
}
